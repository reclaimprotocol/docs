---
title: Installation
description: Welcome to the guide on integrating Reclaim Protocol into your Node.js backend project! This guide will walk you through the installation process and help you get started quickly.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

# Installing the Reclaim Protocol SDK for Backend

Welcome to the guide on integrating Reclaim Protocol into your backend project! This guide will walk you through the installation process for both JavaScript and Python implementations.

## Prerequisites

Before you begin, make sure you have the appropriate environment set up:

For JavaScript:
- Node.js (version 14 or later)
- npm (usually comes with Node.js) or yarn

For Python:
- Python (version 3.7 or later)
- pip (Python package manager)

## Installation Steps

<Tabs items={['JavaScript', 'Python']}>
  <Tabs.Tab>
    ### Installing JavaScript SDK

Open your terminal and navigate to your project directory. Then run one of the following commands:

Using npm:
```bash
npm install @reclaimprotocol/js-sdk
```

Using yarn:
```bash
yarn add @reclaimprotocol/js-sdk
```

    #### Verify Installation

    Check your `package.json` file. You should see `@reclaimprotocol/js-sdk` listed in the `dependencies` section.

    #### Importing the SDK

    ```javascript
    const { ReclaimProofRequest } = require('@reclaimprotocol/js-sdk');
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ### Installing Python SDK

    Open your terminal and run:

    ```bash
    pip install reclaim-python-sdk==0.1.5
    ```

    #### Verify Installation

    You can verify the installation by running:
    ```bash
    pip show reclaim-sdk
    ```

    #### Importing the SDK

    ```python
    from reclaim-python-sdk import ReclaimProofRequest
    ```
  </Tabs.Tab>
</Tabs>

## Next Steps

Congratulations! You've successfully installed the Reclaim Protocol SDK. Here's what you can do next:

1. Check out the [Backend Example](./backend-example) guide to create your first proof request on the server-side.
2. Learn about [Advanced Configuration](../advance-configurations/advance-options) options to customize the SDK for your needs.

If you encounter any issues during installation, please refer to our [FAQ](https://reclaimprotocol.org/faq) or join our [Telegram community](https://t.me/protocolreclaim) for support.

Happy coding with Reclaim Protocol on your backend!
