import { Cards, Card } from 'nextra/components'
import MakeIcon from '../components/MakeIcon'
import SdkCard from '../components/SdkCard'
// import { CardsIcon, OneIcon, WarningIcon } from '../../icons'
import Image from 'next/image'

# Reclaim Protocol SDKs
<p className='text-blue-300 italic'>Build apps upon user data from around the internet</p>
<br></br>

<p className=''>With Reclaim SDKs, you can leverage cryptographic proofs to get user data from other apps. For example, you can get Uber Diver Ratings, Number of Tinder Matches and many more to build powerful applications or impactful marketing campaigns. </p>
<br></br>
<p>Reclaim Protocol uses ZK Proofs and HTTPS Session Keys to securely retrieve user data. This data is stored only on the user’s device, completely invisible to any server, including those owned by Reclaim. [More details of how the protocol works](https://link.reclaimprotocol.org/whitepaper-draft)</p>
<br/>
<p className=''>Step into the future of data empowerment with Reclaim SDKs and create next-generation applications that redefine user experiences.</p>
 
### Integrate Reclaim Protocol in your products
<div>
<br></br>
<Cards>
    <SdkCard title='NodeJs' subtitle='Integrate on backend' link='node' iconName='node' tag='Recommended'/>
    <SdkCard title='React' subtitle='Integrate on your website' link='react' iconName='reactjs' tag='Fastest'/>
    <SdkCard title='React Native' subtitle='Integrate in your mobile app' link='react-native' iconName='react_native' tag="Doesn't need Reclaim Wallet"/>
    <SdkCard title='Flutter' subtitle='Integrate in your mobile app' link='flutter/quickstart' iconName='flutter' tag="Doesn't need Reclaim Wallet"/>
    <SdkCard title='Solidity' subtitle='Integrate in your dapp' link='solidity' iconName='solidity' tag="Recommended"/>
</Cards>
</div>