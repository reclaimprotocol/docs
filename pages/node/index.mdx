import Image from 'next/image'

## Overview

Use Reclaim SDK in your backend server to get access to self-sovereign user data.

**Here is a high-level overview of how your backend server can integrate with Reclaim SDK:**

![sequence](/assets/Reclaim_SDK_integration_updated_group.svg)

## Prerequisites

- Node.js 18.0.0 or later

## Installation

```bash copy
npm i @reclaimprotocol/reclaim-sdk
```

## Initialize the SDK

```js copy
import { reclaimprotocol } from '@reclaimprotocol/reclaim-sdk' 

const reclaim = new reclaimprotocol.Reclaim()  // 👈 initialise Reclaim class
```