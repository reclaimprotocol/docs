import { Steps, Callout } from 'nextra/components';
import Reclaim from '../components/Reclaim';
import Image from 'next/image'
import Link from 'next/link'

## Create a proof of your GitHub contributions

<Steps>
### Generate verification link

<br></br>

<div className="flex flex-col gap-4">
<p> Click on the button below to generate a verification link. This link will be used to verify your GH contributions.</p>
<Reclaim/>
</div>

 
### Generate proof on Reclaim app
The above url will redirect you to the Reclaim app where you can create a proof of your GitHub contributions
<br></br>
<Image src='/assets/generate_proof.jpg' width={300} height={500}/>

### Submit proof

Once the proof is generated, click on `Submit` button on the app to submit the proof. Once the proof is submitted, the orange color will change to green.
<br></br>
<Image src='/assets/submit_proof.jpeg' width={300} height={500}/>
</Steps>

<Callout>
Get verified self-sovereign user data using <Link href='react' className='underline'> @reclaimprotocol/reclaim-client-sdk </Link>
</Callout>